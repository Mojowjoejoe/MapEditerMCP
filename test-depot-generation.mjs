import fs from 'node:fs/promises';
import path from 'node:path';
import net from 'node:net';
import {spawn} from 'node:child_process';
import {createHash} from 'node:crypto';
import {fileURLToPath,pathToFileURL} from 'node:url';
import assert from 'node:assert/strict';
import {Client} from '@modelcontextprotocol/sdk/client/index.js';
import {StdioClientTransport} from '@modelcontextprotocol/sdk/client/stdio.js';
import {resolveEditorRoot} from './editor-root.mjs';

const here=path.dirname(fileURLToPath(import.meta.url)),root=resolveEditorRoot();
const exe=path.resolve(process.argv[2]??'');
if(!process.argv[2])throw new Error('Pass the private editor executable.');
const {createBlankProject}=await import(pathToFileURL(path.join(root,'lib/wulfram.ts')));
const {BUILD_AREAS_KEY}=await import(pathToFileURL(path.join(root,'lib/build-areas.ts')));
await fs.mkdir(path.join(here,'outputs'),{recursive:true});
const out=await fs.mkdtemp(path.join(here,'outputs','depot-generation-native-'));
const fixture=createBlankProject('Depot generation lab',129);fixture.terrain.worldWidth=16000;fixture.terrain.worldHeight=12000;
const fixturePath=path.join(out,'depot-lab.json');await fs.writeFile(fixturePath,JSON.stringify(fixture));
const hash=async p=>createHash('sha256').update(await fs.readFile(p)).digest('hex');
const report={passed:false,executable:exe,executableSha256:await hash(exe),fixture:fixturePath,fixtureSha256:await hash(fixturePath),out};
const sessions=path.join(out,'sessions'),profile=path.join(out,'profile');
const listener=net.createServer();await new Promise(r=>listener.listen(0,'127.0.0.1',r));const port=listener.address().port;await new Promise(r=>listener.close(r));
const probe=async(fn,label)=>{const end=Date.now()+45000;while(Date.now()<end){const value=await fn();if(value)return value;await new Promise(r=>setTimeout(r,150));}throw new Error(`Timeout: ${label}`);};
let app,client,socket;let sequence=0;const pending=new Map();
try{
 app=spawn(exe,[],{windowsHide:true,stdio:'ignore',env:{...process.env,WULFRAM_FORGE_MCP:'1',WULFRAM_MCP_SESSION_DIR:sessions,WULFRAM_FORGE_USER_DATA_DIR:profile,WULFRAM_FORGE_REMOTE_DEBUGGING_PORT:String(port)}});
 app.on('error',e=>{report.spawnError=e.message;});
 const target=await probe(async()=>{try{return(await fetch(`http://127.0.0.1:${port}/json`).then(r=>r.json())).find(t=>t.url==='https://wulfram-forge.local/index.html');}catch{return undefined;}},'editor target');
 socket=new WebSocket(target.webSocketDebuggerUrl);await new Promise((r,j)=>{socket.onopen=r;socket.onerror=j;});
 socket.onmessage=e=>{const m=JSON.parse(e.data),p=pending.get(m.id);if(p){clearTimeout(p.timer);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result);}};
 const send=(method,params={})=>new Promise((resolve,reject)=>{const id=++sequence,timer=setTimeout(()=>{pending.delete(id);reject(new Error(`CDP timeout: ${method}`));},15000);pending.set(id,{resolve,reject,timer});socket.send(JSON.stringify({id,method,params}));});
 const evaluate=async expression=>{const r=await send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});if(r.exceptionDetails)throw new Error(JSON.stringify(r.exceptionDetails));return r.result.value;};
 await probe(()=>evaluate('!!window.wulframMcp && !!document.querySelector(\'input[type="file"][multiple]\')'),'editor ready');
 const doc=await send('DOM.getDocument'),input=await send('DOM.querySelector',{nodeId:doc.root.nodeId,selector:'input[type="file"][multiple]'});
 await send('DOM.setFileInputFiles',{nodeId:input.nodeId,files:[fixturePath]});
 client=new Client({name:'depot-generation-native',version:'1'});await client.connect(new StdioClientTransport({command:process.execPath,args:['--experimental-strip-types',path.join(here,'server.mjs')],env:{...process.env,WULFRAM_MCP_SESSION_DIR:sessions},stderr:'pipe'}));
 const call=async(name,args={})=>{const r=await client.callTool({name,arguments:args});if(r.isError)throw new Error(r.content[0].text);return JSON.parse(r.content[0].text);};
 const session=await probe(async()=>{const r=await call('list_editor_sessions');return r.sessions.find(s=>s.ready&&s.name===fixture.name);},'imported hill');const sessionId=session.sessionId;
 const snapshot=()=>evaluate(`(()=>{const s=window.wulframMcp.dispatch({action:'get_editor_state'});return window.wulframMcp.dispatch({action:'get_snapshot',expectedRevision:s.revision});})()`);
 const before=await snapshot(),state=await call('get_editor_state',{sessionId});
 const request={activeLayoutId:before.project.activeBaseLayoutId,layoutId:'mcp-depot',style:'distributed-depot',seed:'native-depot',placement:{size:'small',x:8000,y:3000,rotation:0,radius:4000,targetCount:0,terrainAware:false}};
 const preview=await call('generate_base_layout',{sessionId,expectedRevision:state.revision,previewOnly:true,request});
 assert.equal(preview.layout.entities.length,22);assert.deepEqual(await snapshot(),before);
 for(const args of [{expectedRevision:'stale',request},{expectedRevision:state.revision,request:{...request,placement:{...request.placement,targetCount:12}}}]){
  const bad=await client.callTool({name:'generate_base_layout',arguments:{sessionId,previewOnly:false,...args}});assert.equal(bad.isError,true);assert.deepEqual(await snapshot(),before);
 }
 await call('generate_base_layout',{sessionId,expectedRevision:state.revision,previewOnly:false,request});
 const applied=await snapshot();assert.equal(applied.undoCount,before.undoCount+1);assert.equal(applied.project.baseLayouts.length,before.project.baseLayouts.length+1);assert.deepEqual(applied.project.terrain,before.project.terrain);assert.deepEqual(applied.project.baseLayouts[0],before.project.baseLayouts[0]);
 const layout=applied.project.baseLayouts.at(-1);assert.deepEqual(layout.entities,preview.layout.entities);assert.deepEqual(layout.metadata,preview.layout.metadata);
 const favorite=await client.callTool({name:'capture_formation_favorite',arguments:{sessionId,expectedRevision:applied.revision,activeLayoutId:layout.id,favoriteId:'blocked-depot'}});assert.equal(favorite.isError,true);assert.deepEqual(await snapshot(),applied);
 await call('undo',{sessionId,expectedRevision:applied.revision});assert.deepEqual((await snapshot()).project,before.project);
 const click=async(label,scope='body')=>{await probe(()=>evaluate(`(()=>{const matches=[...document.querySelectorAll(${JSON.stringify(scope)})].flatMap(s=>[...s.querySelectorAll('button')]).filter(b=>(b.textContent.trim()===${JSON.stringify(label)}||b.getAttribute('aria-label')===${JSON.stringify(label)}||b.getAttribute('title')===${JSON.stringify(label)})&&!b.disabled&&b.getClientRects().length);if(matches.length!==1)return false;matches[0].click();return true;})()`),'button '+label);};
 await click('Browse base library');
 await evaluate(`(()=>{const input=document.querySelector('[aria-label="Search base library"]');Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set.call(input,'Distributed Depot');input.dispatchEvent(new Event('input',{bubbles:true}));})()`);
 await click('Details: Distributed Depot');
 assert.ok(await evaluate(`document.querySelector('.base-library-detail').textContent.includes('Creative')`));
 await click('Preview on current map');
 assert.equal(await evaluate(`!!document.querySelector('[aria-label="Target structures per team"]')`),false);
 const guiBefore=await snapshot();await click('Preview formation');
 await probe(()=>evaluate(`(()=>{const b=[...document.querySelectorAll('button')].find(b=>b.textContent.trim()==='Apply formation');return b&&!b.disabled;})()`),'Depot GUI candidate');
 assert.deepEqual(await snapshot(),guiBefore);
 const shotPath=path.join(out,'depot-preview.png');await fs.writeFile(shotPath,Buffer.from((await send('Page.captureScreenshot',{format:'png'})).data,'base64'));
 await click('Apply formation');const guiApplied=await snapshot();assert.equal(guiApplied.undoCount,guiBefore.undoCount+1);assert.equal(guiApplied.project.baseLayouts.length,guiBefore.project.baseLayouts.length+1);assert.deepEqual(guiApplied.project.terrain,guiBefore.project.terrain);
 const guiLayout=guiApplied.project.baseLayouts.at(-1);assert.equal(guiLayout.metadata['formation.style'],'distributed-depot');assert.ok(guiLayout.entities.length>=22);assert.ok(Object.keys(guiLayout.metadata).some(k=>k.startsWith('formation.distributedDepot.')));
 await click('Undo','.history-controls');assert.deepEqual((await snapshot()).project,guiBefore.project);
 report.checks={mcpPreviewPreserved:true,mcpApplyUndo:true,staleRejected:true,unsupportedCountRejected:true,lossyFavoriteRejected:true,creativeCard:true,guiPreviewPreserved:true,guiApplyUndo:true};report.preview={path:shotPath,sha256:await hash(shotPath)};report.passed=true;
}catch(error){report.error=error.stack;process.exitCode=1;}
finally{if(client)await client.close();if(socket)socket.close();if(app)app.kill();for(const p of pending.values())clearTimeout(p.timer);await fs.writeFile(path.join(out,'report.json'),JSON.stringify(report,null,2));console.log(JSON.stringify({passed:report.passed,report:path.join(out,'report.json'),error:report.error},null,2));}
